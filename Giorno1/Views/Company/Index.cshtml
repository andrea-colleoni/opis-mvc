@model IEnumerable<Giorno1Oggetti.Company>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    <!-- questo commento HTML rimane nella pagina generata -->
    @* questo è un commento Razor *@
    @Html.ActionLink("Nuova company", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            Telefono
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            N° dipendenti
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Indirizzo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefono)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroDipendenti)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Indirizzo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CompanyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CompanyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CompanyId })
        </td>
    </tr>
}

</table>
@Html.ActionLink("Pippo Testo", "Pippo", new { id = "ciao", b = "buongiorno" })